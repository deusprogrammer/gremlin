return {
    complete: (context, event) => {
        // On puzzle completion
    },
    fail: (context, event) => {
        // On puzzle failure
    },
    activate: (context, event) => {
        // On puzzle activation
    }
};